{
  "name": "careercard_e2e_tests",
  "version": "1.0.0",
  "description": "tests codeceptjs",
  "main": "index.js",
  "scripts": {
    "all": "codeceptjs run --steps ",
    "login": "codeceptjs run --steps --grep @login",
    "teams": "codeceptjs run --steps --grep @teams",
    "coaching": "codeceptjs run --steps --grep @coaching",
    "reinforcements": "codeceptjs run --steps --grep @reinforcements",
    "settings": "codeceptjs run --steps --grep @settings",
    "dashboard": "codeceptjs run --steps --grep @dashboard",
    "prizes": "codeceptjs run --steps --grep @prizes",
    "signout": "codeceptjs run --steps --grep @signout",
    "members": "codeceptjs run --steps --grep @members"
    
  },
  "author": "oumaima",
  "license": "ISC",
  "devDependencies": {
    "codeceptjs": "^3.3.0",
    "puppeteer": "^13.5.1"
  }
}