{
  "name": "amina_test",
  "version": "1.0.0",
  "description": "tests codeceptjs",
  "main": "index.js",
  "scripts": {
    "all": "npx codeceptjs run --steps",
    "btata": "npx codeceptjs run --steps --grep 'btata'",
    "limouna": "npx codeceptjs run --steps --grep 'limouna'",
    "search": "npx codeceptjs run --steps --grep 'search_codeceptjs'"
  },
  "author": "amina",
  "license": "ISC",
  "devDependencies": {
    "codeceptjs": "^3.3.0",
    "puppeteer": "^13.5.1"
  }
}